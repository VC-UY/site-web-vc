name: Deploy Site

on:
  push:
    branches: [version3.0, main, master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: deployer
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync files to VPS
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          rsync -avz --delete \
            --exclude '.git' --exclude 'node_modules' --exclude '.next' --exclude 'venv' --exclude '__pycache__' \
            -e "ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no" \
            ./ ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/opt/vc-uy/SITE/

      - name: Rebuild and restart Site
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 30m
          script: |
            set -euo pipefail
            cd /opt/vc-uy
            docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.production build site-backend site-frontend
            docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.production up -d site-backend site-frontend
            for i in 1 2 3 4 5 6; do
              curl -sf http://127.0.0.1:8003/health/ && break
              sleep 10
            done
            curl -sf http://127.0.0.1:8003/health/
            curl -sf http://127.0.0.1:13100/ >/dev/null
            echo "Site OK"

      - name: Smoke test public URL
        run: curl -sf https://vc-uy.npe-techs.com/api/health/
